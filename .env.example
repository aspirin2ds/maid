POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_PORT=5432
POSTGRES_DB=maid

REDIS_USER=maid
REDIS_PASSWORD=redis
REDIS_PORT=6379

# Use "localhost" when running `bun run dev` on your host machine.
# Docker Compose services automatically override these to "postgres"/"redis".
POSTGRES_HOST=localhost
REDIS_HOST=localhost
BETTER_AUTH_URL=http://localhost:3000
AUTH_ORIGIN=http://localhost:3000
# PORT=3000

# CLI helpers
# MAID_BASE_URL=http://localhost:3001
# MAID_ID=cli
# MAID_AUTH_TOKEN_FILE=~/.maid-auth-token

OPENAI_API_KEY=
OPENAI_RESPONSE_MODEL=gpt-4o-mini

OLLAMA_BASE_URL=http://127.0.0.1:11434
OLLAMA_GENERATE_MODEL=llama3.1
OLLAMA_EMBEDDING_MODEL=nomic-embed-text
# OLLAMA_KEEP_ALIVE=-1m

# APP_SHUTDOWN_TIMEOUT_MS=10000
# LOGGER_FILE=logs/current.log
# LOGGER_FILE_SIZE=10m

# MEMORY_QUEUE_NAME=memory-extraction
# MEMORY_QUEUE_JOB_NAME=memory.extract
# MEMORY_QUEUE_DEBOUNCE_DELAY_MS=3000
# MEMORY_QUEUE_ATTEMPTS=3
# MEMORY_QUEUE_BACKOFF_DELAY_MS=1000
# MEMORY_QUEUE_WORKER_CONCURRENCY=1

# MEMORY_SERVICE_DEFAULT_LIMIT=5
# MEMORY_SERVICE_DEFAULT_THRESHOLD=0.7
# MEMORY_EXTRACTION_THRESHOLD=0.7
# MEMORY_EXTRACTION_TOP_K=5
# MEMORY_EXTRACTION_RETRIES=3

# MIGRATIONS_TIMEOUT_MS=30000
# MIGRATIONS_RETRY_DELAY_MS=500
# MIGRATIONS_FOLDER=./drizzle
