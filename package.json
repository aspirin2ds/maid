{
  "name": "maid",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "start": "bun run src/index.ts",
    "test": "bun test",
    "test:real-env": "bun test test/websocket-real-env.test.ts",
    "test:unit": "bun test --preload ./test/setup.mts test/unit",
    "start:infra": "docker compose up -d postgres redis",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate"
  },
  "dependencies": {
    "dotenv": "^17.2.4",
    "drizzle-orm": "^0.45.1",
    "hono": "^4.11.9",
    "ioredis": "^5.9.2",
    "postgres": "^3.4.8"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "drizzle-kit": "^0.31.9",
    "testcontainers": "^11.11.0"
  }
}
